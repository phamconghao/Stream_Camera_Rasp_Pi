diff --git a/CMakeLists.txt b/CMakeLists.txt
index f9af6b6..b3650ca 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -42,10 +42,19 @@ pkg_check_modules(
 #
 pkg_check_modules(
     LIBSRTP2
-    REQUIRED
+    QUIET
     libsrtp2
 )
 
+if(NOT LIBSRTP2_FOUND)
+    message(FATAL_ERROR
+        "libsrtp2 development files are required to build camera_app.\n"
+        "On Raspberry Pi OS/Debian install them with:\n"
+        "  sudo apt update && sudo apt install -y libsrtp2-dev\n"
+        "Then rerun ./build.sh."
+    )
+endif()
+
 #
 # Include directories
 #
diff --git a/README.md b/README.md
index 02719f2..c142cc2 100644
--- a/README.md
+++ b/README.md
@@ -43,8 +43,17 @@ Each pipeline stage owns an independent `static std::atomic<bool>` running flag
 - CMake ≥ 3.16
 - GCC with C++17 support
 - `libcamera` development package (`libcamera-dev`) discoverable via `pkg-config`
+- OpenSSL development package (`libssl-dev`) discoverable via `pkg-config`
+- libSRTP 2 development package (`libsrtp2-dev`) discoverable via `pkg-config`
 - `pthread`
 
+On Raspberry Pi OS/Debian, install the build dependencies with:
+
+```bash
+sudo apt update
+sudo apt install -y build-essential cmake pkg-config libcamera-dev libssl-dev libsrtp2-dev
+```
+
 ## Build
 
 ```bash
